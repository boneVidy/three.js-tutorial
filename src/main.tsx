import React from 'react'
import ReactDOM from 'react-dom/client'
import Pipe from "./geometry/Pipe";


ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
    <Pipe />
)
